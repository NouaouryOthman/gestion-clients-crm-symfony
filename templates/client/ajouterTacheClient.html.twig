{% extends 'base.html.twig' %}

{% block title %}Ajouter Tache{% endblock %}

{% block body %}
<h1>Ajouter une Tache pour le Client N°{{client.id}}</h1>
{{form_start(form)}}
{{form_widget(form)}}
    </br>
<button type="submit" class="btn btn-success">Ajouter</button>
<a class="btn btn-primary" href="{{path('client',{'id':client.id})}}">Retour</a>
{{form_end(form)}}
{% endblock %}